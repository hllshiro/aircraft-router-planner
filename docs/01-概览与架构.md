# 01 — 项目概览与系统架构

> 系列文档第 1 篇。面向技术人员：先看本篇建立全局认知，再按需深入各模块文档。
> 配套设计文档：`docs/技术方案.md`（v0.20，终审定稿，含全部共识/裁决记录）。本文是"设计蓝图 × 实现现状"的映射。

## 1. 项目是什么

**AircraftRouterPlanner** 是一个**确定性 3D 航路规划 CLI 引擎**（Rust 实现，2024 edition）：输入蓝方兵力/武器、红方雷达/地空导弹部署、禁飞/限飞区、地形数据与飞行器性能参数，输出每架飞机的航路点集（经纬度 + MSL 高度）。

核心定位一句话：

> 在保证自身安全（不撞山、不越禁飞/限飞区、满足机动约束）的前提下，规划进入**武器发射阵位**（而非贴目标点）的航路，并规避红方雷达探测。

### 1.1 关键特性速览

| 特性 | 说明 |
|------|------|
| 分层解耦 | 粗层 FMM 代价场（定走廊）→ 细层运动基元/平滑（保可行）→ 全链复验 |
| 确定性 | 固定种子逐位可复现（跨平台），构建期禁 FMA + 固定 target-cpu |
| 性能硬指标 | 每百公里 ≤ 3s（端到端，典型值），绝对不能崩溃（B9） |
| 多机支持 | `vehicles` 数组，一次代价场共享、每机独立 FMM 回溯 |
| 多源地形 | SRTM/HGT、GeoTIFF、DTED/DTED2、自建 ARPK1 紧凑格式（Zstd） |
| 威胁建模 | 球形雷达 + 探测概率衰减 + LOS 地形遮蔽 + 压制修正 + 多雷达概率并集 |
| 输出契约 | `status` 四态（success / degraded_timeout / no_solution / input_invalid）+ `schema_version` |
| 交付形态 | 单二进制（stdin/stdout JSON 管道）+ 默认地形/掩膜文件；`install/` 完整安装包另含可独立运行的 Demo 子包（核心发布物不含 Demo） |

### 1.2 历史背景（重构动机）

项目前身 `simple-router-planner`（CLI 二进制 `rplan-cli`，v0.4.0）为纯 Rust 的确定性 3D 航路规划引擎，采用"宏观拓扑路由 → 安全飞行走廊 → 加权 A\*"三层架构。实测存在硬伤：

| 类别 | 问题 | 后果 |
|------|------|------|
| 性能 | 100km+ 多雷达场景超时 | 宏路由中间点导致某段走廊内无解 → 回退**无约束搜索**（O(N³) 灾难） |
| 物理正确性 | 路径钻入地下（-4500m） | 无地形/地面约束，A\* 把"从雷达球体下方穿过"视为合法 |
| 功能缺口 | 限飞区无法表达、固定翼/旋翼不区分、输出不平滑、无地形输入、蓝方武器未实现 | 多项原始需求未覆盖 |

结论：问题出在算法与实现，不在语言。基于原始功能需求重构，命名 **AircraftRouterPlanner**。

## 2. 目标与硬指标

| 指标 | 定义 | 出处 |
|------|------|------|
| 性能 B8 | 每百公里 ≤ 3s（端到端 wall-clock：进程启动 → stdout 输出可用结果集）；航程按比例线性外推（300km ≤9s、1000km ≤30s），无隐藏封顶 | 方案 5.1 |
| 崩溃 B9 | 绝对不能崩溃；崩溃 = panic/段错误/OOM；合法处置 = 合法错误码 + JSON 输出 | 方案 5.2 |
| 确定性 | 相同种子输出逐位一致（跨平台）；构建期 IEEE754 严格语义 + `-C target-feature=-fma` + 固定 target-cpu | 方案 5.2 |
| 交付 | 单二进制（Linux musl / Windows MSVC 静态）+ 内置地形数据文件；纯 Rust 零 C 依赖 | 方案 1.3 / 3.2.1 |

**验收口径**：`每百公里 ≤ 3s` 为典型值。双基准验收——① 法定基准：真实 SRTM + 多雷达负载；② 交付基准：主管提供的内置地形数据包（全球 152.87m 或中国 76.44m，二选一）。验收只看端到端，不做每过程精细子预算（环节占比仅为初始分配参考）。

## 3. 技术选型

### 3.1 语言：Rust（2024 edition）

Rust 满足三条硬性需求：性能（无 GC、零成本抽象）、确定性（无未定义行为）、交付（cargo 交叉编译生态）。2024 edition 由主管拍板（2026-08-04）：
- `unsafe_op_in_unsafe_fn` 默认 deny，契合 B9"绝对不能崩溃"；
- 不触及浮点语义，跨平台逐位一致仍由 `-fma` 控制；
- 唯一前提：构建工具链 ≥ 1.85（本机 1.95 满足）。

### 3.2 依赖清单（核心 8 + 数据源 3，全部纯 Rust）

| 库 | 用途 | 版本锁定 |
|----|------|---------|
| serde / serde_json | JSON 契约 | ^1 |
| rand | 确定性随机（FNV-1a 噪声场等） | ^0.10（geo 非可选依赖，勿降 0.8） |
| nalgebra | 3D 向量/矩阵基元 | 0.35（默认特性，禁 blas） |
| rstar | R-Tree 空间索引 | ^0.12（geo 锁死，勿升 0.13） |
| geo（含 geo-types） | 测地线距离/方位 | 0.33.1 |
| thiserror | 错误类型 | ^2（dted2 带 ^1，双版本可接受） |
| clap | CLI 参数 | 4.6（derive） |
| geotiff | 运行时 GeoTIFF 读取 | 0.1（锁具体版本） |
| dted2 | 运行时 DTED/DTED2 读取 | 1.0.0 |
| ruzstd | 纯 Rust Zstd 解码（ARPK1 内置格式） | 0.7 |
| sha2 | ARPK1 数据 SHA-256 校验 | 0.10 |

**静态编译红线（3.2.1）**：① nalgebra 仅默认特性禁 blas；② geo 不启用 proj feature（投影自研）；③ flate2 保持 rust_backend；④ ruzstd 纯 Rust（不引 libzstd C）。CI 用 `cargo tree -e normal` 断言零 C 依赖 + musl target 构建一票否决。

**GDAL 定位**：仅开发期离线预处理工具（任意 DEM → 重采样/裁剪/填洞/重投影），不进运行时。

## 4. Cargo Workspace 结构

```
AircraftRouterPlanner/
├── Cargo.toml            # workspace 根（members = cli, phase0, demo/server；resolver 2；edition 2024）
├── .cargo/config.toml    # 确定性构建 flags（-C target-feature=-fma；MSVC +crt-static）
├── cli/                  # ★ 核心 CLI（lib + bin）——正式工程
│   ├── Cargo.toml        # 依赖 + bench（b_load_decompress）
│   ├── src/
│   │   ├── main.rs       # stdin/stdout 管道 + clap 参数 + 解算编排（260 行）
│   │   ├── lib.rs        # 模块声明（17 行）
│   │   ├── config.rs     # 输入/输出 JSON 契约 + InputValidator + 默认参数表（1285 行）
│   │   ├── coord.rs      # 坐标系统：椭球/TM/UTM/GK3/WebMercator/ENU/垂直基准（471 行）
│   │   ├── error.rs      # thiserror 错误类型 + JSON 错误码（134 行）
│   │   ├── costfield.rs  # 代价场 + FMM 传播 + 回溯（561 行）
│   │   ├── threat.rs     # 雷达威胁模型（384 行）
│   │   ├── spatial.rs    # rstar 空间索引：雷达/圆区域（226 行）
│   │   ├── path.rs       # 路径类型 + 几何工具（208 行）
│   │   ├── dubins.rs     # Dubins CSC 最短路径基元（303 行）
│   │   ├── smooth.rs     # 平滑策略链 + 全链复验器（2024 行）
│   │   ├── solver.rs     # 端到端 solver（4492 行，含测试）
│   │   └── terrain/      # 地形数据源（约 3450 行）
│   │       ├── mod.rs    # TerrainSource trait + Sample 语义 + LOS + 退化统计 + 目录分发（490 行）
│   │       ├── builtin.rs# ARPK1 自建紧凑格式（959 行）
│   │       ├── mask.rs   # GSHHG 海陆掩膜 + MaskedSource（503 行）
│   │       ├── srtm.rs   # SRTM/HGT 裸格式 + 目录片级 LRU（285 行）
│   │       ├── geotiff.rs# GeoTIFF（589 行，含 north-up 方向判定 resolve_sy）
│   │       ├── dted.rs   # DTED/DTED2 + 目录片级 LRU（243 行）
│   │       ├── tiledir.rs# 片级目录源框架（DTED/SRTM 共用，M4/M5，263 行）
│   │       ├── convert.rs# ARPK1 转换命令（M2，1078 行）
│   │       └── memory.rs # 内存网格 Terrain（测试/原型，196 行）
│   ├── benches/b_load_decompress.rs
│   ├── examples/         # arpk1_bench / arpk1_probe / mask_probe 等调试工具
│   └── tests/
│       ├── crash_suite.rs      # B9 崩溃/退化输入套件（517 行）
│       ├── e2e_beijing*.json    # 端到端场景输入
│       ├── regress_phase0.rs    # 回归 runner（285 行）
│       └── regression/          # 回归用例集（cases/*.json）+ README
├── phase0/               # 性能原型与基准工程（已并入 workspace 保持可复跑）
│   ├── src/              # dubins.rs / fmm.rs / terrain.rs / lib.rs（早期原型）
│   ├── benches/          # b1_fmm ~ b5_dubins（b6/b7 真实 DEM 基准已于 2026-08-08 删除）
│   ├── scripts/          # 数据预处理 Python 脚本（DEM 分析/压缩/掩膜生成）
│   └── data/             # （2026-08-08 已清理迁移到项目根 data/：默认/成品数据 + GSHHG）
├── data/                 # 地形/掩膜数据（gitignore）：默认 east_asia_7p5as.arpack + mask_7p5as.mask
│                         #   （china_dem_l12 / gmted2010 备用数据已于 2026-08-10 退出测试流程）
├── install/              # 发布版安装包（gitignore）：CLI exe + data/ + demo/ 独立可运行子包
├── demo/                 # 开发期可视化工具（含 install/demo/ 独立打包）
│   ├── server/           # Rust + Axum（POST /api/plan、/api/terrain + 静态文件服务 web-dist）
│   └── web/              # React 18 + TS + Vite + Three.js（npm）
├── docs/
│   ├── 技术方案.md        # v0.20 设计文档（权威设计蓝图，573 行）
│   ├── phase0_baseline.md # Phase 0 基准与实测数据
│   └── 01~11 *.md        # ★ 本系列文档（设计 × 实现现状）
└── start_demo.bat        # Windows 一键启动 Demo
```

## 5. 分层架构（核心设计）

针对旧项目"A\* 均匀网格 + 运动学事后剪枝"的失败模式，新架构采用**分层解耦**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 粗层：FMM 快速行进法（2D 水平网格 + 50m 固定层高语义）              │
│   代价场 = 地形（硬阈值+软代价）× 禁飞/限飞硬墙 × 雷达代价          │
│   → 输出：全可达域（单次传播）+ 可行走廊（回溯）                    │
│   → 多机共享同一传播场（一次 FMM 多目标回溯）                      │
├─────────────────────────────────────────────────────────────────┤
│ 细层：走廊内运动基元/平滑链（运动学约束前置保证可行）                │
│   Theta* 去锯齿 → Catmull-Rom 样条 → Dubins 拟合 → 贪心抽稀        │
│   受限区剖面（底部穿行 / 顶部绕飞）与绕行弧由 solver 切分           │
├─────────────────────────────────────────────────────────────────┤
│ 后处理：全链复验门（verify_path）                                  │
│   {地形净空} ∪ {禁飞/限飞包含} ∪ {转弯半径/转角/航向连续/爬升角}     │
│   ∪ {弦高} ∪ {A6 物理自洽} ∪ {雷达累计探测概率（软告警）}           │
│   任一失败 → 回退上一策略环节；全链失败 → 交付原始折线 + 显式告警    │
├─────────────────────────────────────────────────────────────────┤
│ 回退层：无解出口链（不回归无约束搜索）                              │
│   ① 压制重算（仅显式声明）→ ② 探测概率穿越评估 → ③ 放宽目标半径     │
│   → ④ 走廊细分/重路由 → ⑤ 明确报错（no_solution + 原因码）          │
└─────────────────────────────────────────────────────────────────┘
```

### 5.1 为什么是 FMM 而不是 A\*

方案 4.4 的澄清：A\* 是图搜索算法，与维度无关；真正不适合的是"均匀网格离散化 + 运动学事后剪枝"。FMM 的优势：

| 维度 | FMM（定案主方案） |
|------|------------------|
| 可达域 | 单次传播给出**全场**可达域，直接命中"到达 B 点一定范围"需求 |
| 多机 | 代价场全场复用（架构级红利），每机只做一次 O(NlogN) 回溯 |
| 确定性 | 无启发式误差、确定性堆实现 |
| 地形 | 天然进代价函数（不撞山 + 软代价） |

**网格维度定稿（十三轮共识）**：2D 水平网格 + 50m 固定层高垂直分层（不自适应，层数 = ceil(任务高度范围/50m) + 安全余量）；粗层水平 1-2km（当前实现 `--grid 256` 自适应任务区域）、细层水平 152.87m（数据分辨率）；`z_resolution_m: 50` 写入数据契约。

### 5.2 关键架构决策（共识速查）

| 决策 | 内容 | 实现状态 |
|------|------|---------|
| 运动学约束前置 | 固定翼/旋翼机各自基元；旋翼机悬停/极小转弯半径 r→0 显式建模为合法机动，禁止除零退化 | ✅ 已实现（smooth_options_for + 复验机型分支） |
| 速度为核心输入 | 速度影响 ρ = v²/(g·tan φ_max)、爬升率、雷达暴露时间、发射包线 | ✅ 已实现（A6 自洽校验） |
| 发射阵位 = 区域 | TargetZone 支持区域对象；到达 = 进入区域 ∧ 落点 ∈ [Rmin,Rmax] ∧ 发射包线 | ✅ 已实现（P7 2026-08-12：发射阵位 = 目标点 + 武器射程换算环形目标集——FMM 传播到环带 [Rmin,Rmax] 即停（停在进入环带处/Rmax 外边界）；交付前终检硬校验落点 ∈ 环带 ∧ heading/alt 包线（不满足 → no_solution），speed 包线软校验（degradation）；终端 heading 下放平滑级（Dubins 吃终端 pose）） |
| 多机契约 | vehicles 数组；一次 FMM 多目标回溯；代价场缓存按源键共享；3s 预算按机线性 | ✅ 已实现（共享 field，每机独立 FMM） |
| 多机交叉检测 | 输出后处理检测空间交叉，检出显式告警 | ✅ 已实现（P6-C 2026-08-12：段-段空间接近 h<1km ∧ v<150m → 两车 warnings 各一条 `multi_vehicle_conflict`；时间维 out-of-scope） |
| 无解出口链 | ①压制 → ②穿越评估 → ③放宽半径 → ④细分/重路由 → ⑤报错 | ✅ 已实现（P8 2026-08-13：③ 点目标回溯失败 → 目标半径放宽（10km 上限）+ degradation；④ 粗层 FMM no path → 网格细分重试（grid×2 上限 1024）+ degradation；细分后仍无解 → 几何无解分类；② 穿越评估为直线直穿替代 + 告警；① 压制重算） |
| status 契约 | success / degraded_timeout / no_solution / input_invalid + 原因码 | ✅ 已实现（input_invalid 全量；degraded_timeout 自 P6-B 起真正可触发——3s 预算硬护栏 + warm best-so-far；no_solution 链路完备） |
| 输出坐标口径 | 经纬度（度，WGS84）+ MSL 米（x=经度 y=纬度） | ✅ 已实现 |

## 6. 端到端数据流

```
stdin / --input JSON
   │
   ▼
main.rs: read_input → Input::from_json_str（畸形 JSON → input_invalid: malformed_json）
   │
   ▼
main.rs: config::validate（InputValidator：A=B / B在禁飞区 / 雷达∩禁飞区 / 车辆参数 A6 自洽）
   │  └─ 失败 → input_invalid + 原因码（不进入算法）
   ▼
solver::solve（核心链路）：
   1. TerrainSource 构建（none 平面 / ARPK1 文件）
   2. 车辆规格提取（vehicles 空 → 默认单机 v1）
   3. 任务区域计算（起点+target 包围盒 + 0.15° 缓冲，方形）
   4. Zone 集合合并（no_fly + restricted + obstacles）+ CircleIndex
   5. 语义代价场（Land/Water/Lake=1.0，NoData/OOB=5x 通行；NoFly/Obstacle 硬墙 → Sample::Forbidden INF）
   5c. 禁飞墙膨胀（物理转弯半径 0.5×，clamp [2km,10km]）+ 过渡带软罚（BFS 距离变换 2 格）
   5b. 雷达静态代价（膨胀半径内 cost ×(1+200×(并集概率+深穿惩罚))）
   5d. 地形高度过滤（按本机飞行高度把「Land+净空 ≥ alt+300m」格点置 INF → FMM 水平绕山，防低空穿山回退）
   6. 每机：分段 FMM（start → mid_waypoints → target，共享代价场）→ 回溯
      → 受限区剖面切分（底部/顶部决策，锚点避开相邻 restricted 带、圆求交 r+slack 补偿）
      → 平滑链（段边界转角弧过渡）→ 全链复验 → 输出契约
      → 路径撞山 → 抬升巡航高度到「路径地形最高+净空+100m」重跑（路径级抬升，warnings 提示）
   │
   ▼
stdout / --output JSON（schema_version + status + vehicles[].path + stats）
```

## 7. 实施进度与现状（截至 2026-08-11）

| Phase | 内容 | 状态 |
|-------|------|------|
| Phase 0 | 性能原型与算法验证（FMM/代价场复用/rstar/射线/Dubins/真实 DEM） | ✅ 完成（基准见 docs/phase0_baseline.md） |
| Phase 1 | 核心骨架：workspace + cli lib/bin + JSON 契约 + coord + terrain 4 实现 + costfield + rstar + error/clap + InputValidator | ✅ 完成（S1–S9 + S10 测试回归收尾；此后 P2–P6 每轮全量回归门禁持续：lib/crash/compare/determinism/regress） |
| Phase 2 | 核心算法分层 + 掩膜集成 + 空洞分层语义 | ✅ 完成（S1–S9 语义场；ARPK1 语义位图 S5 于 2026-08-13 关闭——arpack+mask 两文件定案 + MaskedSource 覆盖，见 docs/04 §10） |
| Phase 3 | 输出美化链：Theta* / 样条 / Dubins / 抽稀 / 复验器 / 策略链 | ✅ 完成（2026-08-11 起平滑链移入 fmm_attempt 循环 + 段级地形抬升重跑） |
| Phase 4 | 需求补全：M1 端到端 pipeline / M2 限飞区高度层 / M3 雷达模型 / M4 机型分流 / M5 多段路径拼接 | ✅ M1–M5 全部完成 |
| Phase 5 | 工程化（CI/性能回归/发布） | ✅ 完成（P6-A 2026-08-12：scripts/check.sh + perf_regress.sh + release.sh + .github/workflows/ci.yml；P6-B 3s 预算硬护栏见 §5 差异表，docs/07 §5、docs/11 §8） |
| Demo | 开发期可视化工具（Axum + React/Three.js） | ✅ 完成（含 ARPK1 地形可视化、多边形编辑、必经点拾取/拖动、install/demo 独立打包、外部地形数据范围渲染） |

### 7.1 已知占位/未实现项（新文档与设计方案的差异）

| 项 | 设计（技术方案） | 当前实现 |
|----|----------------|---------|
| 探测概率基准 base_p | 真实雷达参数标定 | ✅ 已标定（2026-08-13 方案 A：Swerling I 典型监视雷达模型，Pfa=1e-6 → VT=13.8155，R_eff 处 base_p=0.9 = 90% 探测距离，SNR₀=130.1≈21.1dB；中心概率推导→1；与 P_cross 解耦不变） |
| LOS mask 代价场 | 传播前预计算 mask 乘子 | 有意不进静态代价场（solver.rs:507 设计注释）：LOS 动态遮挡由 verify 威胁评估精确判定（真实地形 + 逐点采样）；P8 M5（2026-08-13）加 build_cost_field 语义验证测试确认静态场不含 LOS 乘子（不误伤/不重复惩罚），LOS 语义维持 verify 精确判定 |
| Rmin–Rmax 环带目标集 | FMM 末端目标集 | ✅ 已实现（P7 2026-08-12：有武器 → FMM 回溯目标 = 环带内 T 最小可达 cell（传播到环带即停）；环带不可达 → 点目标回退仅当 Rmin≈0，否则 no_solution；Rmin 未定（≤0）显式告警） |
| 多边形受限区剖面 | 支持 | ✅ 已实现（2026-08-12：圆/多边形同语义——底部=alt_min−500m 恒优 / 顶部=alt_max+500m / 都不可行才水平绕行；`line_polygon_inside_bands` 穿行带判据，凹多边形多带支持） |
| 3D Dubins | 评估后接入/自研 | 2D Dubins + 高程剖面独立插值（方案风险表已接受此降级） |
| 多机路径交叉检测 | 输出后处理检测 | ✅ 已实现（P6-C 2026-08-12：`detect_multi_vehicle_crossings`，段-段最近距离 h<1km ∧ v<150m → warnings 显式告警，时间维 out-of-scope） |
| 武器默认表 | 类型 + 按类型默认射程 | ✅ 已定案（P6-D 2026-08-12：WeaponType aam/agm/bomb + 默认射程 [5,40]/[3,120]/[1,15]；类型缺省 = 不启用；威胁建模消费后续） |
| SRTM 空洞插值 | 邻近插值兜底 | 未实现（依赖数据源已填洞） |

## 8. 文档地图

| 文档 | 内容 |
|------|------|
| 02-输入输出契约 | JSON schema、InputValidator、status 四态、错误码、默认参数表 |
| 03-坐标与投影 | 椭球表、TM 正反算、UTM/GK3/WebMercator、ENU、垂直基准 |
| 04-地形数据源 | TerrainSource trait、五种数据源、ARPK1 格式、掩膜、LOS |
| 05-代价场与FMM | CostField、语义代价场、Godunov 迎风差分、回溯、rstar 索引 |
| 06-威胁模型 | 球形雷达、探测概率曲线、LOS、压制、多雷达并集 |
| 07-求解器与端到端流程 | solve() 全流程、多机、禁飞膨胀、受限区剖面、直线替代 |
| 08-路径平滑与复验 | 平滑策略链、Dubins CSC、全链复验清单、机型分支 |
| 09-演示应用 | Axum 后端 API、React/Three.js 前端、启动方式 |
| 10-测试与验证 | 单元/集成/崩溃套件/回归/基准/确定性验证 |
| 11-工程化与构建 | workspace、依赖锁定、静态编译红线、确定性 flags、发布形态 |
